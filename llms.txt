# TinyCloud Docs

TinyCloud docs are organized around reader intent and Diataxis. Start at `/`
to choose a path, then move into concepts or reference when you need the
system model or exact action names.

Full documentation corpus: https://docs.tinycloud.xyz/llms-full.txt

## Agent access quickstart

TinyCloud MCP is the separate `@tinycloud/mcp` package, installed alongside
`@tinycloud/cli`. It is a local stdio server, not a hosted HTTP endpoint and
not a subcommand embedded in `tc`. Both packages require Node.js 20 or newer.

The MCP data surface can discover account spaces and applications; list, read,
inspect, create, replace, upsert, and delete selected KV values; inspect one
SQLite schema; run one bounded read-only SQLite `SELECT`; run one constrained
SQLite `INSERT`, `UPDATE`, or `DELETE`; and read a delegated secret. Generic KV
access blocks the protected `account` and `secrets` spaces. Treat file-like
values according to the owning app's manifest and schema.

Install the stable packages and create a delegated agent profile:

```sh
npm install --global @tinycloud/cli @tinycloud/mcp
tc profile create agent --posture delegate-session --operator agent
```

A fresh delegate profile must bootstrap once. The delegate emits one exact
account-registry request:

```sh
tc --profile agent --quiet --json auth request \
  --cap tinycloud.kv:account:spaces/:get,list \
  --emit request.json
```

An authenticated owner reviews and grants that exact artifact, then the
delegate imports the returned delegation:

```sh
tc --profile owner --quiet --json auth grant request.json --yes > delegation.json
tc --profile agent --quiet --json auth import delegation.json
```

Configure the MCP client to start the server with that explicit profile:

```json
{
  "mcpServers": {
    "tinycloud": {
      "command": "tinycloud-mcp",
      "args": ["--profile", "agent"]
    }
  }
}
```

Use these exact data tools after selecting the delegated profile:

- `tinycloud_account_spaces_list` - list the canonical account space registry
- `tinycloud_account_applications_list` - list registered apps and manifests
- `tinycloud_kv_list` - list keys in one selected space or prefix
- `tinycloud_kv_get` - read one selected KV key
- `tinycloud_kv_head` - read metadata and the ETag for one selected KV key
- `tinycloud_kv_put` - create, ETag-replace, or explicitly upsert one selected KV key
- `tinycloud_kv_delete` - delete one selected KV key, optionally guarded by its ETag
- `tinycloud_sql_schema_inspect` - inspect one exact SQLite database
- `tinycloud_sql_query` - run one bounded `SELECT` against one exact SQLite database
- `tinycloud_sql_execute` - run one positional-parameterized `INSERT`, `UPDATE`, or `DELETE`

KV values are capped at 1 MiB. Base64 is the default lossless representation;
UTF-8 and JSON are explicit tagged alternatives. Use `create` to prevent
overwrite, or read an ETag with `tinycloud_kv_head` and pass it to `replace` or
delete for optimistic concurrency. On a retryable conditional conflict, read
the current ETag before retrying. Deleted keys disappear from reads and lists
immediately, while retained historical blob bytes count toward quota until
garbage collection reclaims them. SQL query defaults are 100 rows and 1 MiB;
hard maxima are 1,000 rows and 4 MiB. Oversized SQL results fail without silent
truncation, and SQL BLOBs use tagged base64 values.

`tinycloud_sql_execute` is destructive and non-idempotent. It requires at least
one positional `?`, one `params` value per placeholder, and
`acknowledgeDatabaseWideAuthority: true`. The tool accepts only one `INSERT`,
`UPDATE`, or `DELETE`, but its requested `tinycloud.sql/write` capability grants
full read, write, and schema-mutation authority over the exact database outside
the narrower tool. This authority is not table- or row-scoped.

When a tool returns `authority_required`, preserve its returned
`tinycloud.auth.request` exactly. The owner grants that artifact without
widening it, the delegate imports the full delegation with
`tinycloud_auth_import`, and the agent retries the original tool.

Complete readable pages:

- MCP reference (Markdown): https://docs.tinycloud.xyz/cli/mcp.md
- Delegated exploration guide (Markdown): https://docs.tinycloud.xyz/guides/access-and-explore-my-tinycloud.md

## Entry points

- `/` - Homepage and reader-journey selector
- `https://docs.tinycloud.xyz/guides/access-and-explore-my-tinycloud` - MCP-first delegated discovery, safe KV CRUD, and SQLite operations
- `/start/browser-app` - Web SDK path from sign-in to first KV write
- `/start/backend-service` - Node SDK path and sign-in mode decision
- `/start/protocol` - Capability, manifest, and delegation vocabulary
- `/start/operator` - CLI, hosting, OpenKey, and troubleshooting path

## Build and understand

- `/quickstart/web-sdk`
- `/quickstart/node-sdk`
- `/guides/authentication`
- `/guides/browser-e2e-testing` - Browser E2E account bootstrap without passkey automation
- `/guides/spaces`
- `/guides/kv-storage`
- `/guides/delegations`
- `/guides/sharing`
- `/guides/data-vault`
- `/guides/write-hooks`
- `/guides/sql-schema-and-migrations`
- `/guides/tinycloud-vfs`
- `/guides/agent-readable-apps`
- `/concepts/system-model`
- `/concepts/security-and-trust`
- `/concepts/did-formats`
- `/concepts/capabilities`
- `/concepts/app-manifests`
- `/reference/glossary`
- `/reference/protocol/capabilities`

## Operate

- `/cli/overview`
- `https://docs.tinycloud.xyz/cli/mcp` - Local stdio MCP server with 16 delegated session, KV, SQL, and secret tools
- `/cli/account` - Read-only live account space and application discovery commands
- `/cli/authentication`
- `/cli/storage`
- `/cli/sql`
- `/cli/duckdb`
- `/cli/spaces`
- `/cli/secrets`
- `/cli/delegations`
- `/cli/auth-artifacts`
- `/cli/profiles`
- `/cli/status`
- `/cli/manifest`
- `/cli/diagnostics`
- `/hosting/overview`
- `/hosting/configuration`
- `/hosting/docker`
- `/hosting/databases`
- `/hosting/storage`
- `/hosting/monitoring`
- `/openkey/index`
- `/openkey/quickstart`
- `/openkey/widget`
- `/openkey/oauth`
- `/openkey/tinycloud-integration`
- `/troubleshooting`

## Source and validation

- Preview locally with `mint dev`.
- Keep `reference/protocol/capabilities.mdx` synced with `npm run sync-capabilities`.
- Verify the generated registry with `npm run check-capabilities`.
